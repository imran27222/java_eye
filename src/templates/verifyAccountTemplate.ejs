<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Your Account</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9fafc;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            width: 90%;
            max-width: 400px;
            background: #ffffff;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
            margin: auto;
        }

        .container h1 {
            font-size: 24px;
            color: #2c3e50;
            margin-bottom: 15px;
        }

        .container p {
            font-size: 14px;
            color: #7f8c8d;
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            width: 100%;
            padding: 12px;
            background-color: #3498db;
            color: #ffffff !important; /* Force white text */
            font-size: 14px;
            font-weight: bold;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #2980b9;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                width: 95%; /* Make the container take more width */
                padding: 20px; /* Adjust padding */
            }

            .container h1 {
                font-size: 20px; /* Slightly smaller heading for mobile */
            }

            .container p {
                font-size: 13px; /* Adjust text size */
            }

            .btn {
                font-size: 13px; /* Slightly smaller button text */
            }
        }

        @media (max-width: 480px) {
            .container {
                width: 90%; /* Make the container smaller for very small devices */
                padding: 15px; /* Adjust padding further */
            }

            .container h1 {
                font-size: 18px; /* Reduce heading size further */
            }

            .container p {
                font-size: 12px; /* Smaller text for small screens */
            }

            .btn {
                font-size: 12px; /* Smaller button text */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Verify Your Email</h1>
        <p>We’re excited to have you join us! Your OTP is </p>

         <p><%= otp %></p>
    </div>
</body>
</html>

